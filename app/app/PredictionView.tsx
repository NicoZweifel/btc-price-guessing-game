import { getRedisClient } from "@/clients";


export type PredictionViewProps = ComponentProps<"div"> & {
  player: string;
};

async function PredictionView({ player }: PredictionViewProps) {
 
  return (
     );
}

export default PredictionView;
